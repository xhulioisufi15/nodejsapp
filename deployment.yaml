apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecom-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ecom-backend
  template:
    metadata:
      labels:
        app: ecom-backend
    spec:
      containers:
        - name: ecom-container
          image: xhulioisufi/ecommerce:v1
          ports:
            - containerPort: 3000 
          command: ["node"]
          args: ["server.js"]
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10

